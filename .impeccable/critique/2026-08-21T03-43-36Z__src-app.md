---
score: 6
p0: 2
p1: 2
timestamp: 2026-08-21T03-43-36Z
slug: src-app
---

---

score: 6
p0: 2
p1: 2
---

# Critique — ISATech website (whole site, refine target: `src/app/(static)` + `src/components`)

Run: honest two-assessment critique. Assessment A = design review (source-based; sandbox blocked curl/live CSS). Assessment B = deterministic detector re-run in parent (`detect.mjs --json "src/app/(static)" "src/components"` → exit 2, 7 findings). Browser visualization UNAVAILABLE (no playwright/puppeteer, no native browser tool, read-only sandbox blocked curl). Both assessments isolated, A finished before detector synthesis.

## Assessment A highlights

Design specificity: **Partial (~6/10)** — "authored content, interchangeable scaffolding." Ownable assets: **4H archetype system** (Hustler/Hacker/Hipster/Hound: team, quiz, results, footer), **3D lanyard member-ID render**, KWADRA-TBI / partner logomarks, Codelympics videos. Interchangeable: the stock hero→stats→about→offer→partners→team→contact landing skeleton. Refine-toward-specific is correct; 4H + lanyard are the load-bearing assets.

Heuristics (0–4): status 3, real-world 3, control 2, consistency 2, error-prev 2, recall 3, flexibility 1, aesthetic 2, recovery 2, help 1.
Cognitive load: random hero each load; duplicate "Learn More" → /about (no direct join path above fold); undeclared acronyms (KWADRA TBI, IPMO); duplicated "What do we offer?" home+membership; motto + "founded 2021" absent from hero; 20-question quiz vs "3–5 min" promise.
Emotional journey: peak-at-open (gold kicker) but inconsistent (random video); biggest valley = membership joins through a **bare Google Form in a new tab** (least designed moment at the strongest ask); partnership = Turnstile + 5/hr rate limit; ends strong (quiz result reveal + gold footer band → tell that story earlier).

## Priority issues

- **P0** Membership application abandons the brand shell for a bare external Google Form (new tab) with no guardrails/preview/recovery. Fix: `onboard`.
- **P0** Random hero video picked each mount → non-deterministic first impression, LCP risk, flat `bg-black/50` overlay, no poster fallback. Fix: `distill`.
- **P1** Competing/vague CTAs starve the primary action (two "Learn More" → /about; no direct join above fold; 4 different CTAs across sections). Fix: `clarify`.
- **P1** Semantic/color token violations: hardcoded `bg-gray-300/50` offer cards (home+membership), `text-black md:text-white` lanyard dark/light hack, header `border-b-grey-100` no-op. Fix: `colorize`/`harden`.
- **P2** Logo right-click onContextMenu hijacks browser context menu → redirect /about (hostile, undiscoverable, blocks copy). Fix: `harden`.
- **P2** Contact funnel trust: `info@isatech.com` vs owned `isatech.club`; `/privacy#manage-cookies` anchor unverified; bare "Too many requests" on rate limit. Fix: `harden`.
- **P3** Motto + Est. 2021 absent from hero credibility lane. Fix: `typeset`/`distill`.

## Detector findings (parent re-run, exit 2)

- Bounce easing ×4: `(static)/loading.tsx:32,36,40` + `common/loading-spinner.tsx:63` (likely intentional loaders — low priority).
- Gradient text ×2: `quiz/intro-screen.tsx:42`, `quiz/result-screen.tsx:64` (bg-clip-text + bg-gradient; verify against brand tokens).
- AI color palette ×1: `quiz/result-screen.tsx:24` `from-violet-500` — the "generalist" quiz result purple sits outside the blue/gold brand world. Assess whether to keep as deliberate exception or align to gold.

## Persona red flags

- **Jordan** (first-timer): random hero video, no join path above the fold, unexplained acronyms, motto/founding hidden below fold.
- **Riley** (stress): hardcoded grays break dark mode, email domain mismatch, membership chain dead-ends in new-tab Google Form, context-menu hijack.
- **Casey** (mobile): three stacked header controls, autoplay video burns data, 20-question serial commitment, apply leaves the site on mobile.

## Minor

Quiz intro archetype colors (amber/blue/pink/emerald) diverge from result-screen gradient colors; all nav hrefs append trailing `#` (no active state); NextTopLoader hardcodes `#203c90→#ffac02`; footer lower third stacks too many decorations (reads as ad band); 4H story buried behind the quiz funnel.

## Provocative questions

1. Why is the 4H archetype system — the most distinctive ownable asset — hidden behind a 20-question funnel instead of leading the hero recruitment story?
2. Which audience actually gets the trustworthy experience: members (bare Google Form) vs partners (Turnstile + rate limit)? Intentional?
3. Random hero video per visit — surprise brand moment, or proof the homepage has no single authored thesis?
4. Strip trademarks — is the remaining scaffolding persuasive or just holding the authored moments up?
