/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://isatech.club",
  generateRobotsTxt: true, // (Optional)
  // You can add more advanced options here if needed
  // For example, excluding certain routes:
  // exclude: ['/admin', '/private/*'],
};
