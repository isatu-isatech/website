"use client";

/**
 * Re-exported from the shared location — the pill is generic (quiz result
 * and membership success both use it for kiosk idle resets).
 */
export { IdleCountdown } from "@/components/common/idle-countdown";
